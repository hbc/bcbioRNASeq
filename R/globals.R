globalVariables(".")
legacyMetricsCols <- c(bcbioBase::metadataPriorityCols, "name", "x53Bias")
packageVersion <- packageVersion("bcbioRNASeq")
requiredAssays <- c("counts", "tpm", "length", "normalized")
validCallers <- c("salmon", "kallisto", "sailfish")
