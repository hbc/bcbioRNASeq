% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-plot_correlation_heatmap.R
\docType{methods}
\name{plot_correlation_heatmap,bcbioRNADataSet-method}
\alias{plot_correlation_heatmap,bcbioRNADataSet-method}
\title{Correlation heatmap}
\usage{
\S4method{plot_correlation_heatmap}{bcbioRNADataSet}(object,
  transform = "rlog", method = "pearson", clustering_method = "ward.D2",
  genes = NULL, samples = NULL, interesting_groups = NULL,
  annotation = NULL, title = NULL, ...)
}
\arguments{
\item{object}{Object.}

\item{transform}{String specifying \link{rlog} (\strong{recommended}) or \link{vst}
\link{DESeqTransform} slotted inside the \link{bcbioRNADataSet}.}

\item{method}{Correlation coefficient (or covariance) to be computed.
Defaults to \code{pearson} but \code{spearman} can also be used. Consult the
\code{\link[stats:cor]{stats::cor()}} documentation for more information.}

\item{clustering_method}{Hierarchical clustering method. Accepts the same
values as \code{\link[stats:hclust]{stats::hclust()}}.}

\item{genes}{\emph{Optional}. Character vector of specific gene identifiers to
plot.}

\item{samples}{\emph{Optional}. Character vector of specific samples.}

\item{interesting_groups}{\emph{Optional}. Interesting groups to label with bars
above heatmap. If \code{NULL}, defaults to \code{interesting_groups} defined in the
\link{bcbioRNADataSet}.}

\item{annotation}{\emph{Optional}. Alternative annotation to use. Useful when
plotting more than one column.}

\item{title}{\emph{Optional}. Text to include in plot title.}

\item{...}{Additional arguments, passed to \code{\link[pheatmap:pheatmap]{pheatmap::pheatmap()}}.}
}
\value{
Heatmap plot \code{\link[pheatmap:pheatmap]{pheatmap::pheatmap()}}.
}
\description{
This function calculates a correlation matrix based on gene expression per
sample. By default, this function processes all gene counts per sample to
calculate the corrlation matrix. This behavior can be overrided with the
input of \code{gene} identifier vector. In this case, only the expression of the
desired genes will be used to calculate the correlation matrix.
}
\examples{
data(bcb)
plot_correlation_heatmap(bcb)
}
\seealso{
\code{\link[stats:cor]{stats::cor()}}

Other Heatmaps: \code{\link{plot_deg_heatmap,bcbioRNAResults,missingOrNULL-method}},
  \code{\link{plot_gene_heatmap,bcbioRNADataSet-method}}
}
\author{
Michael Steinbaugh
}
